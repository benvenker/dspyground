# Required for OpenRouter provider (default)
OPENROUTER_API_KEY=your_openrouter_api_key

# Optional legacy: only needed if you want to use Vercel AI Gateway
AI_GATEWAY_API_KEY=your_ai_gateway_api_key_here

# Optional: Used only for enabling voice mode evals
OPENAI_API_KEY=your_openai_api_key
OPENAI_BASE_URL=set_this_if_you_are_using_whisper_from_another_provider

# Optional: Next.js configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
